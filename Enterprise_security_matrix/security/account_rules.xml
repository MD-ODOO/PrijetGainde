<odoo>
    <!-- Employés -->
    <record id="rule_account_move_employee_block" model="ir.rule">
        <field name="name">Account Move - Employee Block</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="groups" eval="[(4, ref('group_employee_restricted'))]"/>
        <field name="domain_force">[('id','=',False)]</field>
    </record>
</odoo>
