<odoo>
    <record id="view_move_form_create_asset_button" model="ir.ui.view">
        <field name="name">account.move.form.create.asset.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <!-- <xpath expr="//header" position="inside">
                <button
                    name="action_create_asset"
                    type="object"
                    string="Créer l'immobilisation"
                    class="btn-primary"
                    invisible="not can_create_asset"/>
            </xpath> -->

        </field>
    </record>
</odoo>